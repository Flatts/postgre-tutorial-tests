--Grouping Sets return multiple groupings based on parameters

SELECT		
	rental_rate	
	,replacement_cost	
	,COUNT(*)	
FROM film		
GROUP BY		
	GROUPING SETS (	
		(rental_rate, replacement_cost)
		,(rental_rate)
		,(replacement_cost)
		,()
		);
